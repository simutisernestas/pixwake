repos:
  - repo: https://github.com/astral-sh/ruff-pre-commit
    rev: v0.12.4
    hooks:
      - id: ruff
        args: [--select, I, --fix]
        files: ^(src|test)/
      - id: ruff-format
        files: ^(src|test)/
